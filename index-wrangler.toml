name = "zip-api"
main = "index-worker.js"
compatibility_date = "2024-12-20"

# 배포 후 도메인 예시:
# https://zip-api.your-account.workers.dev
# 또는 커스텀 도메인: https://zip.maroowell.com

[env.production]
vars = { }

# 환경 변수 없이 작동 (공개 API 사용)
