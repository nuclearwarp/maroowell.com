# login-wrangler.toml
name = "login"
main = "login-worker.js"
compatibility_date = "2026-02-24"

# ✅ 대시보드에서 넣은 env vars 유지 (wrangler가 덮어쓰는 거 방지)
keep_vars = true

[assets]
directory = "./public"
binding = "ASSETS"
