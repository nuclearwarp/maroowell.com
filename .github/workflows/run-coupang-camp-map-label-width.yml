name: Fix camp map label clipping

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/run-coupang-camp-map-label-width.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fix marker label text measurement
        shell: bash
        run: |
          python3 - <<