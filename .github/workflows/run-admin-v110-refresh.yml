name: Run admin access v1.1.0 refresh

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/run-admin-v110-refresh.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Patch admin access
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('public/admin_access.html')
          s=p.read_text()
          s=s.replace('<div class="title">🤖 권한 관리</div>','<div class="title">관리자 권한 관리</div><div class="sub titleSub">가입 승인 · 계정 검색 · 역할/권한 설정</div>',1)
          s=s.replace('<span class="pill" id="loginPill">세션 확인 전</span>','<button class="secondary systemToggle" id="systemToolsToggle" type="button">시스템 도구</button><span class="pill" id="loginPill">세션 확인 전</span>',1)
          s=s.replace('<div class="card">\n      <div class="sectionTitle">관리자 API</div>','<div class="card systemToolsCard isCollapsed">\n      <div class="sectionTitle">시스템 도구</div>',1)
          s=s.replace('<div class="sectionTitle">가입 승인 / 계정 검색</div>','<div class="sectionTitle">가입 승인 · 계정 검색</div><div class="sectionLead">필요할 때만 조회합니다. 페이지 진입만으로 계정 목록을 자동 검색하지 않습니다.</div>',1)
          s=s.replace('<div class="list" id="pendingUserList"></div>\n      <div class="list" id="accountSearchList"></div>','''<div class="resultGrid">
        <section class="resultPanel"><div class="resultHead"><b>승인 대기 결과</b><span>승인 대기 조회 후 표시</span></div><div class="list" id="pendingUserList"><div class="emptyState">아직 조회하지 않았습니다.</div></div></section>
        <section class="resultPanel"><div class="resultHead"><b>계정 검색 결과</b><span>이름 · 이메일 · 쿠팡ID 검색</span></div><div class="list" id="accountSearchList"><div class="emptyState">검색어를 입력하고 계정 검색을 누르세요.</div></div></section>
      </div>''',1)
          s=s.replace('<div class="sectionTitle">현재 권한 목록</div>\n          <div class="muted">이름을 클릭하면 좌측 대상 입력칸에 들어갑니다.</div>\n          <div class="list" id="userList"></div>','<div class="sectionTitle">현재 권한 목록</div><div class="muted">시스템 도구의 “권한 목록”을 눌렀을 때만 불러옵니다. 이름을 누르면 권한 변경 대상으로 선택됩니다.</div><div class="list" id="userList"><div class="emptyState">권한 목록을 아직 불러오지 않았습니다.</div></div>',1)
          css=r'''

    /* v1.1.0 clean admin console */
    :root{--bg:#f5f7fb;--bg2:#eef3f9;--card:#fff;--line:#dbe3ee;--txt:#172033;--muted:#6b7688;--blue:#2563eb;--red:#dc2626;--green:#15803d;--yellow:#a16207;--purple:#7c3aed}
    body{background:linear-gradient(180deg,#f9fbff,#f3f6fa);color:var(--txt)}.wrap{max-width:1320px;padding:22px 20px 48px}.top{position:sticky;top:0;z-index:30;margin:-22px -20px 20px;padding:15px 20px;border-bottom:1px solid var(--line);background:rgba(255,255,255,.94);backdrop-filter:blur(14px)}
    .brand img{width:36px;height:36px}.title{font-size:23px;color:#0f172a;letter-spacing:-.04em}.titleSub{margin-top:3px;font-size:12px}.card{background:#fff;border:1px solid var(--line);border-radius:20px;box-shadow:0 10px 30px rgba(15,23,42,.055);padding:18px 20px;margin-bottom:14px}.sectionTitle{font-size:17px;color:#0f172a;margin-bottom:7px}.sectionLead{font-size:12px;color:var(--muted);line-height:1.55;margin-bottom:14px}
    button{border:1px solid #cbd5e1;background:#fff;color:#334155;box-shadow:0 1px 0 rgba(255,255,255,.8);font-weight:700}button:hover{background:#f8fbff;border-color:#93c5fd}button.ok{background:#ecfdf5;border-color:#bbf7d0;color:#15803d}button.warn{background:#fffbeb;border-color:#fde68a;color:#92400e}button.danger{background:#fff5f5;border-color:#fecaca;color:#b91c1c}button.purple{background:#f5f3ff;border-color:#ddd6fe;color:#6d28d9}
    input,select{background:#f8fafc;border:1px solid #cbd5e1;color:#0f172a}select option{background:#fff;color:#0f172a}.pill{background:#f8fafc;border-color:#dbe3ee;color:#64748b}.layout{grid-template-columns:minmax(360px,.78fr) minmax(0,1.22fr);align-items:start}.userCard{background:#fff;border-color:#e2e8f0;box-shadow:0 3px 12px rgba(15,23,42,.035)}.userCard.admin{background:#f8fbff;border-color:#bfdbfe}.userCard.maroowell{background:#fbfffc;border-color:#bbf7d0}.email{color:#475569}.meta{color:#64748b}
    .badge{background:#f8fafc;border-color:#e2e8f0;color:#64748b}.badge.admin{background:#eff6ff;color:#1d4ed8;border-color:#bfdbfe}.badge.maroowell{background:#ecfdf5;color:#15803d;border-color:#bbf7d0}.badge.dragon{background:#fffbeb;color:#92400e;border-color:#fde68a}.badge.role,.badge.clhi{background:#f5f3ff;color:#6d28d9;border-color:#ddd6fe}.badge.schedule{background:#fff7ed;color:#9a3412;border-color:#fed7aa}.badge.scheduleOff,.badge.clhiOff{background:#fff5f5;color:#b91c1c;border-color:#fecaca}
    .systemToolsCard.isCollapsed{display:none}.systemToggle{min-height:36px}.resultGrid{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:14px}.resultPanel{min-width:0;border:1px solid #e2e8f0;border-radius:16px;background:#f8fafc;padding:12px}.resultHead{display:flex;align-items:flex-start;justify-content:space-between;gap:10px;padding:2px 2px 8px}.resultHead b{font-size:13px;color:#0f172a}.resultHead span{font-size:10px;color:#94a3b8;text-align:right}.emptyState{padding:28px 16px;border:1px dashed #cbd5e1;border-radius:13px;background:#fff;color:#94a3b8;text-align:center;font-size:12px;line-height:1.55}
    .stat{background:#f8fafc;border-color:#e2e8f0}.tableBtn{background:#f8fafc;color:#334155}.tableBtn.active{background:#eff6ff;border-color:#93c5fd}pre{background:#0f172a;color:#dbeafe;border-color:#1e293b}#rawOnlyCard{display:none!important}
    @media(max-width:980px){.resultGrid{grid-template-columns:1fr}.layout,.schemaGrid{grid-template-columns:1fr}.top{position:relative;margin:-22px -20px 18px}.topRight{width:100%}.systemToggle{width:auto!important}.resultHead{display:block}.resultHead span{display:block;margin-top:3px;text-align:left}}
          '''
          s=s.replace('</style>',css+'\n  </style>',1)
          s=s.replace('''  async function checkMe() {
    showRawOnly();
    setStatus("관리자 세션 확인 중...");
''','''  async function checkMe() {
    setStatus("관리자 세션 확인 중...");
''',1)
          s=s.replace('''    setRaw(data);
    setStatus("관리자 확인 완료", "ok");
  }

  async function loadUsers() {
    showRawOnly();
''','''    setStatus("관리자 확인 완료", "ok");
  }

  async function loadUsers() {
''',1)
          s=s.replace('''  try {
    initSupabase();
    checkMe().catch(() => {});
    loadUsers().catch(() => {});
    loadPendingAccounts().catch(() => {});
  } catch (e) {
''','''  $("systemToolsToggle").onclick = () => {
    const card=document.querySelector(".systemToolsCard");
    card?.classList.toggle("isCollapsed");
    $("systemToolsToggle").textContent=card?.classList.contains("isCollapsed")?"시스템 도구":"시스템 도구 닫기";
  };
  try {
    initSupabase();
    $("pendingUserList").innerHTML='<div class="emptyState">아직 조회하지 않았습니다.</div>';
    $("accountSearchList").innerHTML='<div class="emptyState">검색어를 입력하고 계정 검색을 누르세요.</div>';
    $("userList").innerHTML='<div class="emptyState">권한 목록을 아직 불러오지 않았습니다.</div>';
    setStatus("조회할 항목을 선택하세요.");
    checkMe().catch(() => {});
  } catch (e) {
''',1)
          p.write_text(s)
          PY
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add public/admin_access.html
          git commit -m 'Refresh admin access layout and manual search flow'
          git push origin main

# trigger: 2026-08-20