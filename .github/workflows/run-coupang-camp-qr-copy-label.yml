name: Run coupang camp QR copy label patch

on:
  push:
    branches: [main]
    paths:
      - '.github/scripts/patch_coupang_camp_qr_copy_label.py'
      - '.github/workflows/run-coupang-camp-qr-copy-label.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply patch
        run: python3 .github/scripts/patch_coupang_camp_qr_copy_label.py
      - name: Commit patch
        run: |
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/coupang_camp coupang_camp
          git commit -m "Improve camp QR copy image and label"
          git pull --rebase origin main
          git push origin main
